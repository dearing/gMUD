syntax = "proto3";

package mud;

// The greeting service definition.
service Messaging {
  rpc Echo(stream MessageRequest) returns (stream MessageReply) {}
}

message MessageRequest {
    string message = 1;
}
message MessageReply {
    string message = 1;
}